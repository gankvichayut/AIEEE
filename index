<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Futuristic Club | Cyber Neon Theme</title>
    <style>
        /* --- Reset & Base Styles --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            scroll-behavior: smooth;
        }

        body {
            background-color: #030210;
            /* เน้นแดงกับน้ำเงินเป็นหลัก (Red ~40%, Blue ~40%, Purple/Pink ~20%) */
            background-image: 
                radial-gradient(at 15% 20%, rgba(220, 38, 38, 0.45) 0px, transparent 50%),  /* แดง Crimson */
                radial-gradient(at 85% 30%, rgba(37, 99, 235, 0.5) 0px, transparent 50%),   /* น้ำเงิน Royal Blue */
                radial-gradient(at 20% 80%, rgba(2, 132, 199, 0.4) 0px, transparent 50%),   /* น้ำเงินสว่าง Sky Blue */
                radial-gradient(at 80% 75%, rgba(225, 29, 72, 0.4) 0px, transparent 50%),   /* แดง Rose */
                radial-gradient(at 50% 50%, rgba(147, 51, 234, 0.2) 0px, transparent 50%),  /* ม่วงจางๆ */
                radial-gradient(at 50% 90%, rgba(219, 39, 119, 0.2) 0px, transparent 50%);  /* ชมพูจางๆ */
            background-attachment: fixed;
            color: #fff;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* --- Custom Scrollbar --- */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #030210;
        }
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #dc2626, #2563eb); /* ไล่ระดับแดงไปน้ำเงิน */
            border-radius: 4px;
        }

        /* --- Glassmorphism Card Base --- */
        .glass-card {
            background: rgba(255, 255, 255, 0.03);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.4);
            transition: all 0.3s ease;
        }

        .glass-card:hover {
            border-color: rgba(220, 38, 38, 0.5);
            box-shadow: 0 0 25px rgba(37, 99, 235, 0.4), 0 0 10px rgba(220, 38, 38, 0.3);
        }

        /* --- 1. Futuristic Navbar --- */
        .navbar {
            position: fixed;
            top: 15px;
            left: 50%;
            transform: translateX(-50%);
            width: 90%;
            max-width: 1100px;
            padding: 12px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: rgba(3, 2, 16, 0.65);
            border-radius: 50px;
            border: 1px solid rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(16px);
            box-shadow: 0 0 20px rgba(37, 99, 235, 0.25), 0 0 10px rgba(220, 38, 38, 0.2);
        }

        .logo {
            font-size: 1.4rem;
            font-weight: 800;
            background: linear-gradient(45deg, #ef4444, #3b82f6, #ec4899);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-transform: uppercase;
            letter-spacing: 1.5px;
        }

        .nav-links {
            display: flex;
            gap: 25px;
            list-style: none;
        }

        .nav-links a {
            color: rgba(255, 255, 255, 0.85);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            transition: 0.3s;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0%;
            height: 2px;
            background: linear-gradient(90deg, #dc2626, #2563eb);
            transition: 0.3s;
            border-radius: 2px;
        }

        .nav-links a:hover {
            color: #fff;
            text-shadow: 0 0 8px rgba(37, 99, 235, 0.8);
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        /* --- Layout Container --- */
        .container {
            width: 90%;
            max-width: 1100px;
            margin: 0 auto;
            padding: 120px 0 60px 0;
        }

        /* --- 2. Hero Section --- */
        .hero-section {
            text-align: center;
            padding: 60px 20px;
            margin-bottom: 60px;
        }

        .hero-title {
            font-size: 3.2rem;
            font-weight: 800;
            line-height: 1.2;
            margin-bottom: 20px;
            /* เน้นแดงกับน้ำเงินเป็นหลัก */
            background: linear-gradient(90deg, #ef4444 0%, #3b82f6 50%, #a855f7 75%, #ec4899 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.75);
            max-width: 600px;
            margin: 0 auto 30px auto;
        }

        .cta-btn {
            display: inline-block;
            padding: 14px 36px;
            border-radius: 30px;
            background: linear-gradient(45deg, #dc2626, #2563eb);
            color: #fff;
            text-decoration: none;
            font-weight: 600;
            box-shadow: 0 0 20px rgba(220, 38, 38, 0.4), 0 0 10px rgba(37, 99, 235, 0.4);
            transition: 0.3s;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .cta-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 0 30px rgba(37, 99, 235, 0.7), 0 0 15px rgba(220, 38, 38, 0.6);
        }

        /* --- Section Titles --- */
        .section-title {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 40px;
            position: relative;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 60px;
            height: 3px;
            background: linear-gradient(90deg, #dc2626, #2563eb);
            margin: 10px auto 0 auto;
            border-radius: 2px;
        }

        /* --- 3. About Section --- */
        .about-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            align-items: center;
            margin-bottom: 80px;
        }

        .about-text {
            padding: 30px;
        }

        .about-img {
            height: 300px;
            border-radius: 20px;
            background: url('https://images.unsplash.com/photo-1522071820081-009f0129c71c?q=80&w=1000&auto=format&fit=crop') center/cover;
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 0 20px rgba(37, 99, 235, 0.2);
        }

        /* --- 4. Highlights Section --- */
        .highlights-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 80px;
        }

        .highlight-card {
            padding: 30px;
            text-align: center;
        }

        .highlight-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            display: inline-block;
        }

        /* --- 5. Connect With Us Section --- */
        .connect-wrapper {
            max-width: 500px;
            margin: 0 auto;
            padding: 40px 20px;
            text-align: center;
        }

        .banner-img {
            width: 100%;
            height: 140px;
            border-radius: 15px;
            background: url('https://images.unsplash.com/photo-1614728894747-a83421e2b9c9?q=80&w=1000&auto=format&fit=crop') center/cover;
            margin-bottom: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .links-container {
            display: flex;
            flex-direction: column;
            gap: 12px;
            margin-top: 20px;
        }

        .link-button {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 14px;
            text-decoration: none;
            color: #fff;
            font-weight: 600;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.04);
            transition: 0.3s;
            position: relative;
            overflow: hidden;
        }

        /* ตกแต่งแต่ละปุ่มด้วยคู่สี Red-Blue สลับกัน */
        .facebook-btn { border: 1px solid rgba(37, 99, 235, 0.5); }  /* น้ำเงิน */
        .instagram-btn { border: 1px solid rgba(220, 38, 38, 0.5); } /* แดง */
        .line-btn { border: 1px solid rgba(236, 72, 153, 0.5); }      /* ชมพู */
        .email-btn { border: 1px solid rgba(168, 85, 247, 0.5); }      /* ม่วง */

        .link-button:hover {
            transform: translateY(-2px);
            backdrop-filter: blur(8px);
        }

        .facebook-btn:hover { background: rgba(37, 99, 235, 0.25); box-shadow: 0 0 15px rgba(37, 99, 235, 0.5); }
        .instagram-btn:hover { background: rgba(220, 38, 38, 0.25); box-shadow: 0 0 15px rgba(220, 38, 38, 0.5); }
        .line-btn:hover { background: rgba(236, 72, 153, 0.25); box-shadow: 0 0 15px rgba(236, 72, 153, 0.5); }
        .email-btn:hover { background: rgba(168, 85, 247, 0.25); box-shadow: 0 0 15px rgba(168, 85, 247, 0.5); }

        /* --- Footer --- */
        footer {
            text-align: center;
            padding: 20px;
            color: rgba(255, 255, 255, 0.4);
            font-size: 0.85rem;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero-title { font-size: 2.2rem; }
            .about-grid { grid-template-columns: 1fr; }
            .navbar { padding: 10px 20px; }
            .nav-links { gap: 15px; font-size: 0.85rem; }
        }
    </style>
</head>
<body>

    <!-- 1. Futuristic Navbar -->
    <nav class="navbar">
        <div class="logo">CLUB LOGO</div>
        <ul class="nav-links">
            <li><a href="#home">หน้าแรก</a></li>
            <li><a href="#about">เกี่ยวกับเรา</a></li>
            <li><a href="#highlights">จุดเด่น</a></li>
            <li><a href="#connect">ติดต่อเรา</a></li>
        </ul>
    </nav>

    <div class="container">
        
        <!-- 2. Hero Section -->
        <section id="home" class="hero-section">
            <h1 class="hero-title">ยินดีต้อนรับสู่ชมรมของเรา</h1>
            <p class="hero-subtitle">พื้นที่สำหรับการเรียนรู้ สร้างสรรค์ และปลดปล่อยศักยภาพของคนรุ่นใหม่ ร่วมสร้างอนาคตไปพร้อมกับเรา</p>
            <a href="#connect" class="cta-btn">เข้าร่วมชมรมกับเรา</a>
        </section>

        <!-- 3. About Section -->
        <section id="about" style="margin-bottom: 80px;">
            <h2 class="section-title">เกี่ยวกับชมรม</h2>
            <div class="about-grid">
                <div class="glass-card about-text">
                    <h3 style="margin-bottom: 15px; background: linear-gradient(90deg, #ef4444, #3b82f6); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">เราคือใคร?</h3>
                    <p style="color: rgba(255,255,255,0.85);">ชมรมของเราเป็นศูนย์รวมของนิสิต/นักศึกษาที่มีความสนใจร่วมกัน มุ่งเน้นการลงมือปฏิบัติจริง (Hands-on Practice) การแลกเปลี่ยนความรู้ และการสร้างโปรเจกต์นวัตกรรมใหม่ๆ ร่วมกัน</p>
                </div>
                <div class="about-img"></div>
            </div>
        </section>

        <!-- 4. Highlights Section -->
        <section id="highlights">
            <h2 class="section-title">สิ่งที่คุณจะได้รับ</h2>
            <div class="highlights-grid">
                <div class="glass-card highlight-card">
                    <div class="highlight-icon">🚀</div>
                    <h3 style="margin-bottom: 10px; color: #3b82f6;">Hands-on Projects</h3>
                    <p style="font-size: 0.9rem; color: rgba(255,255,255,0.7);">ได้ลงมือทำจริง สร้างผลงานเข้า Portfolio ของตัวเอง</p>
                </div>
                <div class="glass-card highlight-card">
                    <div class="highlight-icon">💡</div>
                    <h3 style="margin-bottom: 10px; color: #ef4444;">Workshops</h3>
                    <p style="font-size: 0.9rem; color: rgba(255,255,255,0.7);">กิจกรรมอบรมความรู้และเทคโนโลยีใหม่ๆ จากรุ่นพี่และผู้เชี่ยวชาญ</p>
                </div>
                <div class="glass-card highlight-card">
                    <div class="highlight-icon">🤝</div>
                    <h3 style="margin-bottom: 10px; color: #ec4899;">Community</h3>
                    <p style="font-size: 0.9rem; color: rgba(255,255,255,0.7);">เจอเพื่อนๆ และรุ่นพี่ที่มีความสนใจตรงกัน พร้อมซัพพอร์ตเสมอ</p>
                </div>
            </div>
        </section>

        <!-- 5. Connect With Us Section -->
        <section id="connect">
            <h2 class="section-title">ช่องทางการติดต่อ</h2>
            <div class="glass-card connect-wrapper">
                <div class="banner-img"></div>
                <h3 style="margin-bottom: 5px;">Connect with Us</h3>
                <p style="font-size: 0.85rem; color: rgba(255,255,255,0.6);">ติดตามข่าวสารและสอบถามข้อมูลเพิ่มเติมได้ผ่านช่องทางนี้</p>
                
                <div class="links-container">
                    <a href="https://facebook.com/yourpagename" target="_blank" class="link-button facebook-btn">Facebook Page</a>
                    <a href="https://instagram.com/yourusername" target="_blank" class="link-button instagram-btn">Instagram</a>
                    <a href="https://line.me/ti/p/@yourlineoa" target="_blank" class="link-button line-btn">LINE Official Account</a>
                    <a href="mailto:your.email@example.com" class="link-button email-btn">Email Us</a>
                </div>
            </div>
        </section>

    </div>

    <!-- Footer -->
    <footer>
        <p>© 2026 Futuristic Club. All rights reserved.</p>
    </footer>

</body>
</html>
